# 疲劳驾驶监测系统 - 技术文档

## 一、系统架构

### 1.1 四层架构模型

```
┌─────────────────────────────────────────────────────────────────────────────┐
│                           显示层 (HealthMonitoringSystem)                      │
│  ┌─────────────┐  ┌──────────────────┐  ┌─────────────────────────────────┐  │
│  │  Gateway    │  │ UnifiedDashboard │  │  BluetoothControl              │  │
│  │  (模块选择)  │  │   (数据可视化)    │  │  (蓝牙数据注入)                  │  │
│  └─────────────┘  └──────────────────┘  └─────────────────────────────────┘  │
└─────────────────────────────────────────────────────────────────────────────┘
                                      │ WebSocket / HTTP
                                      ▼
┌─────────────────────────────────────────────────────────────────────────────┐
│                        通信层 (Vite Proxy)                                    │
│  ┌─────────────────────────────────────────────────────────────────────────┐ │
│  │  前端 :5173  ──代理──▶  /ws/fatigue ──▶ ws://localhost:3002/ws        │ │
│  │         ──代理──▶  /api/bluetooth-data-fatigue ──▶ http://localhost:3002│ │
│  └─────────────────────────────────────────────────────────────────────────┘ │
└─────────────────────────────────────────────────────────────────────────────┘
                                      │
                                      ▼
┌─────────────────────────────────────────────────────────────────────────────┐
│                        服务层 (FatiguedDriving Backend)                       │
│  ┌───────────────────┐  ┌────────────────────┐  ┌────────────────────────┐  │
│  │  Flask Server      │  │  算法模块          │  │  WebSocket广播          │  │
│  │  app.py            │  │  blink_fatigue.py │  │  (15秒心跳机制)         │  │
│  │  端口: 3002        │  │  run_fatigue_pipeline│ │                        │  │
│  └───────────────────┘  └────────────────────┘  └────────────────────────┘  │
└─────────────────────────────────────────────────────────────────────────────┘
                                      │
                                      ▼
┌─────────────────────────────────────────────────────────────────────────────┐
│                        采集层 (Simulated Data)                               │
│  ┌─────────────────────────────────────────────────────────────────────────┐ │
│  │  test_data_injection.py - 疲劳驾驶数据模拟器                              │ │
│  │  3种状态: 正常驾驶 | 疲劳状态 | 信号不稳定                                 │ │
│  └─────────────────────────────────────────────────────────────────────────┘ │
└─────────────────────────────────────────────────────────────────────────────┘
```

---

## 二、核心文件清单

### 2.1 后端文件

| 文件路径 | 说明 | 关键函数/类 |
|---------|------|------------|
| `d:\MyProject\DC\DaChuang\FatiguedDriving\app.py` | Flask服务器入口 | `receive_bluetooth_data()`, `broadcast_fatigue()`, `ws_handler()` |
| `d:\MyProject\DC\DaChuang\FatiguedDriving\algorithm\blink_fatigue.py` | 疲劳检测算法 | `run_fatigue_pipeline()`, `assess_fatigue()`, `detect_blink_events()` |

### 2.2 前端文件

| 文件路径 | 说明 | 关键函数/类 |
|---------|------|------------|
| `d:\MyProject\DC\DaChuang\HealthMonitoringSystem\src\App.tsx` | 主应用入口 | `handleBluetoothDataReceived()`, `handleMessage()` |
| `d:\MyProject\DC\DaChuang\HealthMonitoringSystem\src\hooks\useDynamicWebSocket.ts` | WebSocket管理 | `useDynamicWebSocket()` |
| `d:\MyProject\DC\DaChuang\HealthMonitoringSystem\src\services\dataMapper.ts` | 数据映射层 | `mapFatigueData()`, `mapModuleData()` |
| `d:\MyProject\DC\DaChuang\HealthMonitoringSystem\vite.config.ts` | Vite代理配置 | proxy规则 |

### 2.3 测试文件

| 文件路径 | 说明 |
|---------|------|
| `d:\MyProject\DC\DaChuang\FatiguedDriving\test_data_injection.py` | 疲劳驾驶数据模拟器 |

---

## 三、完整数据传输链路

### 3.1 数据流向图

```
test_data_injection.py (信号生成器)
       │
       │ HTTP POST /api/bluetooth-data
       │ {rawData: [...], timestamp: ms, signalQuality: 95}
       ▼
┌──────────────────────────────────────────────────────┐
│  vite.config.ts 代理配置                              │
│  '/api/bluetooth-data-fatigue' ──rewrite──▶ '/api/bluetooth-data'│
│  target: 'http://localhost:3002'                    │
└──────────────────────────────────────────────────────┘
       │
       ▼
┌──────────────────────────────────────────────────────┐
│  FatiguedDriving/app.py  - /api/bluetooth-data      │
│                                                      │
│  1. 接收数据: body = request.get_json()              │
│  2. 存入bluetooth_data缓冲区 (最多1000条)             │
│  3. 提取rawData加入fatigue_signal_buffer              │
│  4. 滑动窗口: 保持最多30秒数据 (FATIGUE_WINDOW_SECONDS=30)│
│  5. 数据足够时(≥300样本)调用算法                      │
└──────────────────────────────────────────────────────┘
       │
       │ fatigue_signal_buffer (滑动窗口, 最多3000个点)
       ▼
┌──────────────────────────────────────────────────────┐
│  blink_fatigue.py - run_fatigue_pipeline()          │
│                                                      │
│  1. 预处理: adaptive_preprocess_eyelid_signal()      │
│     - 形态学滤波去基线漂移                            │
│     - 低通滤波 (6.5Hz)                               │
│     - 归一化到 [0,1]                                 │
│  2. 特征提取: extract_blink_features()               │
│     - 峰值检测 (height=0.55)                        │
│     - 谷值检测 (找 -x 的峰)                          │
│     - 计算: 频率, 时长, 幅度, 闭合比例               │
│  3. 事件检测: detect_blink_events()                  │
│     - normal: 正常眨眼                               │
│     - long: 长时间眨眼 (>1.5倍平均)                  │
│     - incomplete: 凝视区间 (间隔过长+波动小)          │
│  4. 疲劳评估: assess_fatigue()                       │
│     - 5维度评分加权融合                              │
│     - 输出: 疲劳分数 (0-100) + 等级                   │
└──────────────────────────────────────────────────────┘
       │
       │ fatigue_output dict
       │ {fatigueScore, fatigueLevel, blinkRate, ...}
       ▼
┌──────────────────────────────────────────────────────┐
│  app.py - broadcast_fatigue()                       │
│                                                      │
│  WebSocket广播: {"type": "fatigue", "data": {...}}  │
└──────────────────────────────────────────────────────┘
       │
       │ ws://localhost:3002/ws
       ▼
┌──────────────────────────────────────────────────────┐
│  vite.config.ts WebSocket代理                         │
│  '/ws/fatigue' ──rewrite──▶ '/ws'                   │
│  target: 'ws://localhost:3002'                      │
└──────────────────────────────────────────────────────┘
       │
       ▼
┌──────────────────────────────────────────────────────┐
│  useDynamicWebSocket.ts - useDynamicWebSocket()      │
│  1. 接收消息: onMessage handler                     │
│  2. 解析type: msg.type === 'fatigue'                 │
│  3. 15秒心跳 + 3秒重连机制                          │
└──────────────────────────────────────────────────────┘
       │
       │ WebSocketMessage {type, data}
       ▼
┌──────────────────────────────────────────────────────┐
│  App.tsx - handleMessage()                          │
│  1. 判断 msg.type === 'fatigue'                     │
│  2. 调用 mapModuleData('fatigue', msg.data)         │
└──────────────────────────────────────────────────────┘
       │
       │ UnifiedMetricData
       ▼
┌──────────────────────────────────────────────────────┐
│  dataMapper.ts - mapFatigueData()                   │
│  1. 转换疲劳分数到颜色                               │
│  2. 构建辅助指标                                     │
│  3. 返回统一格式                                     │
└──────────────────────────────────────────────────────┘
       │
       ▼
┌──────────────────────────────────────────────────────┐
│  UnifiedBentoDashboard.tsx                          │
│  显示: mainValue (疲劳分数) + 辅助指标 + 状态         │
└──────────────────────────────────────────────────────┘
```

---

## 四、API端点详解

### 4.1 HTTP API

| 端点 | 方法 | 说明 | 请求体 |
|------|------|------|--------|
| `/api/bluetooth-data` | POST | 接收蓝牙数据 | `{rawData: number[], timestamp: number, signalQuality: number}` |
| `/api/fatigue-latest` | GET | 获取最新疲劳结果 | - |
| `/api/stats` | GET | 获取数据统计 | - |
| `/api/clear-buffer` | POST | 清空数据缓冲区 | - |

### 4.2 WebSocket API

| 路径 | 说明 | 发送消息 |
|------|------|---------|
| `/ws` | WebSocket连接 | `{"type": "fatigue", "data": {...}}` |

---

## 五、算法逻辑详解

### 5.1 预处理 (adaptive_preprocess_eyelid_signal)

```python
def adaptive_preprocess_eyelid_signal(raw_signal, sampling_rate=100):
    # 1) 去直流
    centered_signal = x - np.mean(x)
    
    # 2) 形态学滤波估计基线漂移 (2秒窗口)
    size_drift = int(drift_window_sec * sampling_rate)
    trend_open = grey_opening(centered_signal, size=size_drift)
    trend_base = grey_closing(trend_open, size=size_drift)
    baseline_removed = centered_signal - trend_base
    
    # 3) 低通滤波 (6.5Hz)
    b, a = signal.butter(8, 6.5/nyquist, "lowpass")
    filtered_signal = signal.filtfilt(b, a, baseline_removed)
    
    # 4) 归一化到 [0,1]
    normalized_signal = (filtered_signal - min) / (max - min)
```

### 5.2 眨眼特征提取 (extract_blink_features)

```python
# 关键特征计算：
features = {
    "avg_blink_amplitude": 平均眨眼幅度,
    "max_blink_amplitude": 最大眨眼幅度,
    "avg_blink_interval": 平均眨眼间隔 (峰间距),
    "blink_frequency": 眨眼频率 (次/秒),
    "blink_rate_per_min": 眨眼频率 (次/分钟),
    "avg_blink_duration": 平均眨眼时长 (谷到谷),
    "eye_closure_ratio": 眼闭合比例 (%),
    "eye_status": "open" / "closed" (最近1秒均值判断),
    "signal_quality": 信号质量 (0~1),
}
```

### 5.3 事件分类 (detect_blink_events)

```python
# 分类规则：
if e["duration"] > avg_dur * 1.5:
    classified_blinks["long"].append(e)      # 长眨眼
else:
    classified_blinks["normal"].append(e)    # 正常眨眼

# 不完全眨眼（凝视区间）：
if (interval_duration > normal_interval * 1.5 and 
    max_variation < 0.02):
    classified_blinks["incomplete"].append(e)  # 凝视
```

### 5.4 疲劳评分算法 (assess_fatigue)

```python
# 5维度评分：
weights = {
    "blink_frequency_score": 0.20,      # 眨眼频率
    "blink_duration_score": 0.25,       # 眨眼时长
    "long_blink_ratio_score": 0.15,     # 长眨眼比例
    "incomplete_blink_ratio_score": 0.10, # 不完全眨眼比例
    "eye_closure_score": 0.35,          # 眼闭合比例（权重最高）
}

# 疲劳等级：
if fatigue_score < 20:   fatigue_level = "清醒"
elif fatigue_score < 40: fatigue_level = "轻度疲劳"
elif fatigue_score < 60: fatigue_level = "中度疲劳"
elif fatigue_score < 80: fatigue_level = "重度疲劳"
else:                    fatigue_level = "危险"
```

### 5.5 眼闭合比例计分规则

```python
# 更敏感的计分：
if eye_closure_ratio >= 25: score = 100.0    # 严重疲劳
elif eye_closure_ratio >= 18: score = 85.0   # 重度
elif eye_closure_ratio >= 12: score = 65.0   # 中度
elif eye_closure_ratio >= 8: score = 45.0    # 轻度
elif eye_closure_ratio >= 4: score = (ratio - 4) * 15  # 轻微
else: score = 0.0
```

### 5.6 模拟数据特征

| 状态 | 眨眼频率 | 眨眼类型 | 基线特征 | 疲劳分预期 |
|------|---------|---------|---------|-----------|
| 正常驾驶 | ~12次/分钟 | 短眨眼 (100ms) | 稳定低基线 (0.05) | < 30 |
| 疲劳状态 | 低频 | 长眨眼 + 长时间闭眼 (2s+) | 高基线 (0.55) | > 70 |
| 信号不稳定 | 随机 | 无规律 | 大幅波动 | 数据波动 |

---

## 六、前端数据映射

### 6.1 后端原始数据格式

```typescript
interface BackendFatigueData {
  fatigueScore: number;       // 疲劳评分 (0-100)
  fatigueLevel: string;       // 疲劳等级："清醒" | "轻度疲劳" | "中度疲劳" | "重度疲劳" | "危险"
  blinkRate: number;          // 眨眼频率 (次/分钟)
  avgBlinkDuration: number;   // 平均眨眼时长 (ms)
  alertLevel: string;         // 预警等级
  eyeStatus?: string;         // 眼睛状态
  drivingTime?: string;       // 驾驶时长
}
```

### 6.2 前端统一格式

```typescript
interface UnifiedMetricData {
  mainValue: number;           // 疲劳分数
  mainValueLabel: string;      // "疲劳评分"
  mainValueUnit: string;       // "分"
  mainValueColor: 'green' | 'yellow' | 'red' | 'white';
  secondaryMetrics: SecondaryMetric[];
  chartData: ChartDataPoint[];
  status: {
    connected: boolean;
    signalQuality: number;
    lastUpdate: string;
  };
}
```

### 6.3 分数颜色规则

```typescript
const getScoreColor = (score: number) => {
  if (score < 40) return 'green';   // 精神好 / 轻度疲劳
  if (score < 70) return 'yellow';  // 中度疲劳
  return 'red';                       // 重度疲劳 / 危险
};
```

---

## 七、代理配置 (vite.config.ts)

### 7.1 HTTP代理

```typescript
'/api/bluetooth-data-fatigue': {
  target: 'http://localhost:3002',
  changeOrigin: true,
  rewrite: (path) => path.replace(/^\/api\/bluetooth-data-fatigue/, '/api/bluetooth-data')
}
```

### 7.2 WebSocket代理

```typescript
'/ws/fatigue': {
  target: 'ws://localhost:3002',
  ws: true,
  changeOrigin: true,
  rewrite: (path) => path.replace(/^\/ws\/fatigue/, '/ws')
}
```

---

## 八、启动指令

### 8.1 后端服务 (终端1)

```bash
cd d:\MyProject\DC\DaChuang\FatiguedDriving
python app.py
```

输出:
```
[服务器] 疲劳驾驶后端服务启动中...
   访问地址: http://localhost:3002
   WebSocket: ws://localhost:3002/ws
   API (POST 数据): http://localhost:3002/api/bluetooth-data
```

### 8.2 前端服务 (终端2)

```bash
cd d:\MyProject\DC\DaChuang\HealthMonitoringSystem
npm run dev
```

### 8.3 模拟数据注入 (终端3)

```bash
cd d:\MyProject\DC\DaChuang\FatiguedDriving
python test_data_injection.py
```

---

## 九、调试日志位置

### 9.1 后端日志 (终端1)

| 日志标记 | 含义 |
|---------|------|
| `[DATA] 蓝牙数据接收` | 收到HTTP数据 |
| `[STATS] 数据统计` | 每10次的数据统计 |
| `[ALGO] FATIGUE computed` | 算法计算结果 |
| `[WS] 前端客户端已连接/断开` | WebSocket连接状态 |

### 9.2 前端日志 (浏览器控制台)

| 日志标记 | 含义 |
|---------|------|
| `[App] 收到消息` | WebSocket消息接收 |
| `[App] 处理数据` | 数据处理开始 |
| `[App] 映射后数据` | 数据映射结果 |

---

## 十、关键配置参数

| 参数 | 值 | 说明 |
|------|-----|------|
| `FATIGUE_WINDOW_SECONDS` | 30 | 滑动窗口时长(秒) |
| `SAMPLING_RATE` | 100 | 采样率(Hz) |
| `FATIGUE_MIN_SAMPLES` | 300 | 最小样本数(3秒) |
| `BLUETOOTH_DATA_MAX` | 1000 | 原始数据缓冲区大小 |
| `peak_height` | 0.55 | 峰值检测阈值 |
| `min_peak_distance_sec` | 0.33 | 最小峰间距(秒) |

---

## 十一、与其他系统对比

| 对比项 | 疲劳驾驶 | 干眼症 | 睡眠质量 |
|--------|---------|-------|---------|
| 端口 | 3002 | 3000 | 3001 |
| 算法文件 | blink_fatigue.py | dry_eye.py | sleep_quality.py |
| 测试脚本 | test_data_injection.py | test_dryeye_injection.py | test_sleep.py |
| 数据类型 | 疲劳评分(0-100) | 干眼风险评分(0-100) | 睡眠阶段(0-4)+质量评分 |
| 核心指标 | 眼闭合比例, 眨眼时长 | 不完全眨眼比例 | signal_std, rem_density |
| 风险等级 | 5级(清醒/轻度/中度/重度/危险) | 3级(低/中/高风险) | 5级睡眠阶段 |
| WebSocket类型 | `fatigue` | `dryEye` | `sleepQuality` |

---

## 十二、算法流程图

```
原始信号(raw_signal)
       │
       ▼
┌──────────────────────────────┐
│ 预处理                        │
│  - 去直流                     │
│  - 形态学去基线漂移            │
│  - 低通滤波                   │
│  - 归一化 [0,1]               │
└──────────────────────────────┘
       │
       ▼ normalized_signal
┌──────────────────────────────┐
│ 特征提取                      │
│  - 峰值检测 (peaks)           │
│  - 谷值检测 (valleys)         │
│  - 计算频率/时长/幅度/闭合比例 │
└──────────────────────────────┘
       │
       ▼ features, peaks, valleys
┌──────────────────────────────┐
│ 事件检测与分类                │
│  - normal: 正常眨眼           │
│  - long: 长眨眼               │
│  - incomplete: 凝视区间       │
└──────────────────────────────┘
       │
       ▼ blink_events, classified_blinks
┌──────────────────────────────┐
│ 疲劳评估                      │
│  - 5维度评分                  │
│    * 眨眼频率 (20%)           │
│    * 眨眼时长 (25%)           │
│    * 长眨眼比例 (15%)         │
│    * 不完全眨眼比例 (10%)      │
│    * 眼闭合比例 (35%)         │
│  - 加权融合 → 疲劳分数         │
│  - 等级判定                   │
└──────────────────────────────┘
       │
       ▼ fatigueScore, fatigueLevel
```
